# Chicken Road

Пошаговая crash-игра, где игрок управляет курицей, переходящей дорогу. На каждом шаге нужно выбрать безопасную ячейку, избегая ловушки. Успешный выбор увеличивает коэффициент выигрыша.

## Скриншот

```
┌─────────────────────────────────────┐
│         🐔 Chicken Road             │
├─────────────────────────────────────┤
│  57.67x  │ [?] [?] [?] │  10        │
│  38.44x  │ [?] [?] [?] │   9        │
│  25.63x  │ [?] [?] [?] │   8        │
│  17.09x  │ [?] [?] [?] │   7        │
│  11.39x  │ [?] [?] [?] │   6        │
│   7.59x  │ [?] [?] [?] │   5        │
│   5.06x  │ [?] [?] [?] │   4        │
│   3.38x  │ [?] [?] [?] │   3        │
│   2.25x  │ [?] [?] [?] │   2        │
│   1.46x  │ [?] [?] [?] │   1  ←     │
└─────────────────────────────────────┘
```

## Технологии

- **React 18** + **TypeScript**
- **Vite** — сборка и dev-сервер
- **CSS Modules** — стилизация компонентов
- **Web Crypto API** — provably fair алгоритм

## Установка

```bash
# Клонирование репозитория
git clone <repo-url>
cd chicken-road

# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка для продакшена
npm run build
```

## Игровой процесс

1. **Выберите сложность** — количество ячеек в ряду (2-5)
   - 2 ячейки: 50% шанс, до 1024x
   - 3 ячейки: 66% шанс, до 57x
   - 4 ячейки: 75% шанс, до 17x
   - 5 ячеек: 80% шанс, до 9x

2. **Сделайте ставку** и нажмите "Начать игру"

3. **Выбирайте ячейки** на каждом шаге
   - 🐔 — безопасная ячейка (успех)
   - 🚗 — ловушка (проигрыш)

4. **Заберите выигрыш** в любой момент или продолжайте рисковать

## Структура проекта

```
src/
├── components/
│   ├── Game/
│   │   ├── GameBoard.tsx      # Игровое поле
│   │   ├── GameRow.tsx        # Ряд с ячейками
│   │   └── GameCell.tsx       # Кликабельная ячейка
│   └── Controls/
│       ├── BetPanel.tsx       # Панель ставок
│       └── GameHistory.tsx    # История игр
├── context/
│   └── GameContext.tsx        # Состояние игры (React Context)
├── services/
│   └── mockApi.ts             # Mock API с provably fair
├── utils/
│   ├── coefficients.ts        # Расчёт коэффициентов
│   └── crypto.ts              # HMAC-SHA256 для честности
├── types/
│   └── game.ts                # TypeScript интерфейсы
└── App.tsx                    # Главный компонент
```

## Provably Fair

Игра использует криптографически доказуемую честность:

1. **Server Seed** — генерируется перед игрой, хеш показывается игроку
2. **Client Seed** — может быть задан игроком
3. **Nonce** — уникальный номер раунда

```
trapIndex = HMAC_SHA256(serverSeed, clientSeed + nonce) % cellCount
```

После игры раскрывается Server Seed для верификации.

## Формула коэффициента

```
multiplier = (cellCount / (cellCount - 1))^step × (1 - houseEdge)
```

Где `houseEdge = 3%`

## Скрипты

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск dev-сервера |
| `npm run build` | Сборка для продакшена |
| `npm run preview` | Превью собранного приложения |
| `npm run lint` | Проверка кода линтером |

## Лицензия

MIT
